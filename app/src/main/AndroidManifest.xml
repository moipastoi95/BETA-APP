<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.george.betamdl">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_phoques"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_phoques_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ChooseSchoolActivity"></activity>
        <activity android:name=".userSettings" />
        <activity android:name=".AddClubActivity" />
        <activity android:name=".AddUserActivity" />
        <activity android:name=".ClubDetailActivity" />
        <activity android:name=".event_admin_list" />
        <activity android:name=".testDbActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EventDetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddEventActivity" />
        <activity android:name=".MembersActivity" />
        <activity android:name=".ManageClubActivity" />
        <activity
            android:name=".Info"
            android:label="@string/title_activity_info" />
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".HelloService"
            android:exported="false"
            android:launchMode="singleTop" />

        <activity android:name=".ForgotActivity" />
        <activity android:name=".StatsActivity" />
    </application>

</manifest>